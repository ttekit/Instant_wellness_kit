# 🛸 Drone Delivery Wellness Service

Инновационная платформа для экспресс-доставки велнес-наборов с помощью дронов в черте Нью-Йорка.  
Система включает в себя интерактивную витрину товаров, расчет налогов на основе геопозиции и панель управления заказами.

---

## 🛠 Технологический стек

### Frontend
- **React + Vite** — высокая скорость сборки и современный UI.
- **Tailwind CSS** — адаптивная и быстрая верстка.
- **TypeScript** — строгая типизация для предотвращения ошибок на этапе разработки.

### Backend
- **NestJS** — надежный фреймворк для масштабируемых серверных приложений.
- **Prisma ORM** — удобное управление базой данных и типизация запросов.
- **PostgreSQL** — реляционная база данных для хранения заказов и пользователей.
- **Swagger / OpenAPI** — автоматическая документация всех эндпоинтов.

---

## 🚀 Быстрый запуск

### 1. Требования

- Node.js (v18+)
- PostgreSQL (локально или через Docker)

---

### 2. Настройка базы данных

В папке `backend` создайте файл `.env`:

```env
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/drone_db?schema=public"
```

Выполните команды для инициализации БД:

```bash
cd backend
npm install
npx prisma migrate dev --name init
npx prisma generate
```

---

### 3. Запуск Backend (NestJS)

```bash
npm run start:dev
```

API URL:  
http://localhost:3000  

Swagger UI:  
http://localhost:3000/api  

---

### 4. Запуск Frontend (React)

В папке `frontend` создайте файл `.env`:

```env
VITE_API_BASE_URL=http://localhost:3000
VITE_API_ORDERS_URL=http://localhost:3000/orders
VITE_API_JURISDICTIONS_URL=http://localhost:3000/jurisdictions
```

Запустите сервер разработки:

```bash
cd frontend
npm install
npm run dev
```

App URL:  
http://localhost:5173  

---

## 🏗 Структура базы данных (Prisma)

### ProductPackage
Информация о наборах:
- `package` — название набора
- `price` — цена
- `img_link` — ссылка на изображение
- описание набора

### Order
Данные о заказах:
- `subtotal`
- `total_amount`
- налоги
- текущий статус заказа

### User
- профили пользователей
- пароли
- роли

### Jurisdiction
- координаты (`lat`, `long`)
- используется для определения налоговой ставки в зоне доставки

---

## 💡 Инструкции для разработчика

### Добавление или изменение товара

1. Откройте Prisma Studio:
   ```bash
   npx prisma studio
   ```

2. Перейдите в таблицу `products_packeges`.

3. Поле `img_link` должно содержать прямую ссылку на изображение (URL).

4. Поля `price` и `tax_rate` заполняются числами (Decimal на стороне БД).

---

## 🔐 Управление через API (Админка)

- `GET /product-packeges/with-details`  
  Получение всех товаров с их составом для витрины.

- `PATCH /product-packeges/:id`  
  Обновление данных набора, включая смену состава продуктов.

- `POST /orders`  
  Оформление нового заказа (требует `packageId` и `userId`).

---

## ⚠️ Решение частых проблем

### Ошибка JSON (Unexpected token <)

Проверьте адрес бэкенда в `.env`.  
Если API недоступно или путь указан неверно, сервер возвращает HTML-ошибку (например, 404), которую фронтенд не может распарсить.

### Ошибка 400 Bad Request

Убедитесь, что при заказе передается `packageId`.  
Поле обязательно для валидации на бэкенде.

### Картинки не отображаются

- Убедитесь, что в базе данных в поле `img_link` стоит рабочая ссылка.
- В React должен использоваться `product.img_link`.

---

## 📦 Итог

Drone Delivery Wellness Service — это полноценная full-stack платформа с:

- современным React-интерфейсом  
- масштабируемым NestJS-бэкендом  
- типизированной ORM Prisma  
- PostgreSQL базой данных  
- автоматической документацией через Swagger  

Проект готов к локальному запуску и дальнейшему масштабированию.
